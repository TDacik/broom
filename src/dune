(executable
  (name ContractGenerator)
  (public_name bi)
  (libraries CL biabd)
  (modules ContractGenerator))

(executable
  (name biabductor)
  (public_name bi)
  (libraries core z3 biabd)
  (modules biabductor))

(executable
  (name test)
  (public_name bi)
  (libraries core CL z3 biabd)
  (modules test))
