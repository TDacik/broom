(rule
  (targets Version.ml)
  (action
    (with-stdout-to Version.ml
     (bash "echo \"let curr=\\\"%{version:bi}-`git describe --always`\\\"\n\""))))

(library
  (name biabd)
  (public_name bi.biabd)
  (flags :standard)
  (libraries z3 CL)
  (inline_tests (backend qtest.lib))
)
